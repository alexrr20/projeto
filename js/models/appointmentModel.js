export default class appointmentModel {
	constructor(id, user, testCenter, date, testType) {
		this.id = id;
		this.user = user;
		this.testCenter = testCenter;
		this.date = date;
		this.testType = testType;
	}
}
